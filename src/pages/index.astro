---
// Component Imports
//import Counter from '../components/Counter.vue';

// Full Astro Component Syntax:
// https://docs.astro.build/core-concepts/astro-components/

// Антон Сидельников, [18 Apr 2022 at 14:04:09]:
// ...
// 1. Нужна одна основная страница (считай, лэндинг)
// 2. На странице какой-то статический контект и одна форма для ввода email’ов.
// 3. За формой POST на API, который валидирует данные (проверяет имейлы по базе, валидность имейла и т.д.).
// 4. Соответственно ошибки от API должны динамически высвечиваться (“You email is invalid, good try, haha”).
// 5. Там еще будет раунд валидации мыла через отправку письма
// 6. При валидации почты вспомогательная статическая страница типа “спасибо что зарегались, за вами уже выехали”.
// 7. Поддержка локализации (видимо текстов _и_ ресурсов) - пока на 3 языках (EN/UA/RU).

// Что-то такое в голове.

// (7) - важно, но я думаю это может быть не так просто ) Надо подхватывать язык из GeoIP или куки, так что в любом случае должен быть запрос на сервер при загрузке

// larionov, [18 Apr 2022 at 14:07:09]:
// Для этого можно использовать cloudflare worker между клиентом и cdn, но надо посмотреть сколько это будет стоить

// Возможно пригодится и для защиты от ддос

// Антон Сидельников, [18 Apr 2022 at 14:08:54]:
// Сможешь собрать простой макет на тему 1-6 на Vue? И показать как его собирать/расширять? Для меня это будет очень хороший старт. Если влезет (7) (в любом виде, можно без куки - просто переключать кнопкой один из языков и прикладывать к запросу параметром / хэдером типа X-Request-Lang) - я буду в полном восторге.

// Типа
// 1. Страница с текстом “Стань волонтером”, инпутом и кнопкой по центру красиво.
// 2. POST на сервер JSON’ом и обработка ошибок если провалила валидация.
// 3. Статическая страничка “Спасибо”

// Я не знаю, сколько времени это займет 🙂

---
<script>
  window.location.pathname = `/en/`;
</script>
